URGENT: FIX THE PRICINA OWNER APP PREVIEW/DEVELOPMENT ENVIRONMENT

I cannot currently see or properly run the PRICINA Owner app preview.

The app is not properly visible/working in:
1. Expo Go on Android
2. Expo Web/browser preview

Fix the project completely so I can immediately preview the current app and continue development.

DO NOT just explain the problem. ACTUALLY inspect the project, identify the root cause, fix the files/configuration/dependencies, and verify the preview.

REQUIREMENTS:

1. EXPO GO / ANDROID PREVIEW
- Make the project compatible with the currently supported Expo SDK.
- Check package.json and all Expo/React Native dependencies for version mismatches.
- Make sure Expo Go can load the project without a blank screen, crash, red error screen, or infinite loading.
- Make sure the correct entry point is configured.
- Make sure Expo Router configuration is correct if Expo Router is being used.
- Make sure app.json/app.config is valid.
- Make sure Metro bundler starts successfully.
- Make sure all imports resolve correctly.
- Fix any native-module dependency that is incompatible with Expo Go.
- If a dependency cannot work in Expo Go, replace it with an Expo-compatible implementation for development preview, while keeping the architecture ready for the production Android build.
- Do not remove required Firebase functionality just to make the preview work.

2. WEB PREVIEW
- Make the same project run correctly in Expo Web/browser.
- Fix any React Native Web compatibility issues.
- Fix platform-specific code where Android-only APIs are currently crashing the web preview.
- Use platform guards where required.
- The web preview must show the actual PRICINA Owner UI, not a placeholder screen.
- Navigation must work in the browser.
- Dashboard, leads, lead details and other implemented screens must render properly on web.
- If a native-only feature such as phone dialer, Android notification, or native Firebase functionality cannot execute identically on web, gracefully handle it instead of crashing the entire web app.

3. FIREBASE
- Do NOT remove the existing Firebase integration.
- Preserve the existing PRICINA Firebase project integration.
- Project ID:
  pricina-a47fa
- Preserve the existing Firestore structure and existing website data.
- The app must continue using the same backend.
- If Firebase initialization currently causes the preview to crash, fix initialization rather than replacing Firebase with fake data.
- Do not hardcode fake lead data as a permanent solution.
- If development preview needs mock data temporarily, clearly isolate it behind a development-only fallback and ensure production uses Firestore.

4. ENVIRONMENT CHECK
Run/perform equivalent checks for:
- package.json
- app.json/app.config
- Expo SDK version
- React Native version
- Expo Router version
- Firebase package versions
- Metro configuration
- Babel configuration
- TypeScript configuration
- entry point
- dependency compatibility

Run:
- npx expo-doctor@latest
- npx expo install --fix when appropriate
- npx expo start
- npx expo start --web
- npx expo export

Fix every actual error found instead of ignoring warnings that affect runtime.

5. CACHE / METRO
If the issue is caused by stale Metro/Expo cache:
- clear the appropriate Expo/Metro cache
- restart the development server
- verify again

Use the appropriate equivalent of:
npx expo start -c

Do not leave the project dependent on an old cached state.

6. EXPO GO CONNECTION
Make sure the development server can be reached from the Android phone.

If LAN connection is not working:
- verify the project works with:
  npx expo start --tunnel

The final setup should support normal LAN development and tunnel development when necessary.

7. WEB
Verify:
- browser opens the app
- no blank page
- no fatal console error
- navigation works
- UI renders
- Firebase initialization does not crash the browser
- responsive layout works at mobile and desktop browser sizes

8. ANDROID / WEB PLATFORM SEPARATION
Audit every Android-specific API.

Examples:
- Notifications
- Linking
- Dialer
- WhatsApp intents
- Device APIs
- Secure storage
- biometric APIs
- native modules

For each native-only feature:
- keep the Android implementation
- provide a safe web fallback
- prevent the web build from importing/executing unsupported native functionality in a way that crashes the app.

9. UI PREVIEW
After fixing the environment, make sure I can actually see the current PRICINA Owner UI.

The preview must show the real app screens already implemented, including the current dashboard/owner interface.

Do not replace the interface with:
- Hello World
- Expo starter screen
- blank screen
- temporary placeholder

10. NAVIGATION
Verify all currently implemented routes/screens.

Every route must:
- open
- render
- navigate back correctly
- not crash
- not produce undefined route errors

11. ERROR HANDLING
If something fails because Firebase credentials/configuration are missing from the environment:
- show a useful development error
- do not silently show a blank screen
- explain exactly what configuration is missing
- keep the rest of the UI renderable where possible

12. IMPORTANT
Do not rebuild the entire application from scratch.

Preserve the existing PRICINA Owner implementation and fix the current project.

Do not remove:
- Firebase
- Firestore
- lead architecture
- real-time functionality
- notifications architecture
- security architecture
- no-delete policy
- owner screens
- existing UI work

Only change what is necessary to make the project reliably previewable and buildable.

13. FINAL VERIFICATION

Before reporting completion, verify all of these:

[ ] Expo development server starts successfully
[ ] Expo Go can open the project
[ ] Android preview renders the actual PRICINA Owner UI
[ ] Expo Web starts successfully
[ ] Browser preview renders the actual PRICINA Owner UI
[ ] No blank screen
[ ] No fatal Metro error
[ ] No fatal JavaScript runtime error
[ ] No unresolved imports
[ ] No dependency version mismatch affecting runtime
[ ] Navigation works
[ ] Firebase initializes correctly
[ ] Firestore integration remains intact
[ ] Existing data/schema is preserved
[ ] Android-only features do not crash Web
[ ] Web-only limitations are handled gracefully
[ ] Project can generate a production bundle

Finally, provide a short diagnostic report containing:

1. Root cause of the preview problem
2. Files/configuration changed
3. Dependencies changed
4. What was done for Expo Go
5. What was done for Web
6. Firebase changes, if any
7. Exact commands used to verify
8. Final verification result
9. Any remaining issue

MOST IMPORTANT:
Do not say “fixed” unless you actually verified the preview/build.
The goal is that I can open Expo Go and the Web preview and SEE the PRICINA Owner app immediately.